# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CountryCoordinator' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CountryCoordinator
	
	pod 'PromiseKit/Alamofire', '~> 6.0'
  pod "PromiseKit", "~> 6.0"
	pod 'SwiftyJSON', '~> 4.0'


  target 'CountryCoordinatorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CountryCoordinatorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
